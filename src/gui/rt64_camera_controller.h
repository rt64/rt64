//
// RT64
//

#pragma once

namespace RT64 {
    struct CameraController {
        /*
        hlslpp::int2 lastCursorPos;

        CameraController();
        void moveCursor(GameFrame &gameFrame, const hlslpp::int2 cursorPos, const hlslpp::int2 windowSize);
        void movePerspective(GameFrame &gameFrame, const hlslpp::float3 translation);
        void rotatePerspective(GameFrame &gameFrame, float yaw, float pitch);
        void lookAtPerspective(GameFrame &gameFrame, const hlslpp::float3 position, const hlslpp::float3 focus);
        */
    };
};